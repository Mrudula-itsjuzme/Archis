# Branch note

`intent-preservation-demo` was intentionally created as an experimental branch before the intent-preservation implementation. If `main` advances while this work is in progress, rebase or merge the latest `main` before merging the feature PR. Do not force-update `main` from this branch.
