# scripts

## patches/

One-off Node scripts used earlier to apply bulk edits to files under `src/`. They are kept for history only and are not part of the build. Run from the repository root if you ever need one, since they use root-relative paths (e.g. `node scripts/patches/patch_store.cjs`). Prefer editing source directly.
